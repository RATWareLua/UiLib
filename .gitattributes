# Библиотека тянется через HttpGet прямо из репозитория, поэтому переводы строк
# должны быть одинаковыми на любой машине, а не зависеть от настроек клиента.
*.luau text eol=lf
*.md   text eol=lf
*.ps1  text eol=crlf
